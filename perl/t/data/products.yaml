description: |
  CRUD Hera API for Products. 
head:
  description: Meta information about the state of this resource
get:
  description: List of all products
  queryParameters:
  is: [paged]
  responses: 
    200:
      body:
        application/json: 
          example: !include list.json
    500:
/{productId}:
  head:
    description: List of all products
    queryParameters:
    is: [paged]
    responses: 
      200:
        body:
          application/json: 
            example: !include list.json
  get:
    description: List of all products
    queryParameters:
    is: [paged]
    responses: 
      200:
        body:
          application/json: 
            example: !include list.json
  put:
    description: List of all products
    queryParameters:
    is: [paged]
    responses: 
      200:
        body:
          application/json: 
            example: !include list.json
  patch:
    description: List of all products
    queryParameters:
    is: [paged]
    responses: 
      200:
        body:
          application/json: 
            example: !include list.json